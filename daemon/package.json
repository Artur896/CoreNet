{
  "name": "corenet-daemon",
  "version": "0.1.0",
  "description": "CoreNet provider daemon — watches the chain and executes compute jobs",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc",
    "start:js": "node dist/index.js"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/web3.js": "^1.98.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}
